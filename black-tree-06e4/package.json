{
  "name": "black-tree-06e4",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pages:dev": "wrangler pages dev --compatibility-date=2023-05-24 --proxy 5173 -- npm run dev",
    "pages:deploy": "npm run build && wrangler pages publish ./dist"
  },
  "dependencies": {
    "vue": "^3.3.2"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "vite": "^4.3.5",
    "wrangler": "^3.0.1"
  }
}